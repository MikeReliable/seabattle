package com.example.seaBattle.repos;

import com.example.seaBattle.objects.Field;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FieldRepo extends JpaRepository<Field, Long> {
}
